name: elanel_asistencia_it
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  animate_do: ^4.2.0
  cloud_firestore: ^5.6.9
  cloud_functions: ^5.5.2
  collection: ^1.19.1
  file_picker: ^10.2.0
  firebase_auth: ^5.6.0
  firebase_core: ^3.14.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  flutter_markdown: ^0.7.7+1
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.1
  http: ^1.4.0
  http_parser: ^4.1.2
  intl: ^0.20.2
  markdown_toolbar: ^0.5.0
  mime: ^2.0.0
  mobile_scanner: ^7.0.1
  permission_handler: ^12.0.0+1
  qr_flutter: ^4.1.0
  timeago_flutter: ^3.7.0
  timeline_tile: ^2.0.0
  vibration: ^3.1.3
  video_player: ^2.10.0

dev_dependencies:
  change_app_package_name: ^1.5.0
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/elanel.png" # ← Cambialo por tu imagen real
  adaptive_icon_background: "#121318"
  adaptive_icon_foreground: "assets/images/elanel.png" # opcional para Android 8+
  remove_alpha_ios: true
flutter:
  uses-material-design: true
  assets:
    - assets/images/
